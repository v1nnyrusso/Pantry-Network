<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <!-- CSS and JavaScript links can be included here -->
    <style>
        /* Container Styles */
        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            margin: 0;
            font-family: Arial, sans-serif;
        }

        .container-1 {
            min-width: 900px;
            min-height: 500px;
            margin: 20px auto;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 10px;
            overflow-x: auto;
            position: relative;
            z-index: 1;
        }

        /* Table Styles */
        .table {
            width: 100%;
            margin-top: 20px;
        }

        .table th,
        .table td {
            padding: 8px;
            border-bottom: 1px solid #ddd;
            text-align: left;
        }

        .table th {
            background-color: #f2f2f2;
        }

        .table tbody tr:hover {
            background-color: #f5f5f5;
        }

        /* Footer Styles */
        footer {
            background-color: #f8f9fa;
            padding: 20px 0;
            width: 100%;
            position: sticky;
            bottom: 0;
            z-index: 0;
        }
    </style>
</head>

<body>
    <div class="container-1">
        <h2 style="text-align: center; color: #000000"> {{pantry}} Claimed Products</h2>

        <table id="userTable" class="table">
            <thead>
                <tr>
                    <th>Product Name</th>
                    <th>Quantity</th>
                    <th>Expiry</th>
                    <th>Date Donated</th>
                    <th>Donator's Name</th>
                    <th>Donator's Email</th>
                    <th>Donator's Phone Number</th>
                </tr>
            </thead>
            <tbody id="userTableBody">
                {{#each claimedDonations}}
                {{#each products}}
                <tr>
                    <td>{{productName}}</td>
                    <td>{{quantity}}</td>
                    <td>{{expiry}}</td>
                    <td>{{../donationDate}}</td>
                    <td>{{donatedByUser.firstName}} {{donatedByUser.secondName}}</td>
                    <td>{{donatedByUser.email}}</td>
                    <td>{{donatedByUser.number}}</td>
                </tr>
                {{/each}}
                {{/each}}
            </tbody>
        </table>
        <p>
            {{#if errorMessage}}
            <div class="alert alert-danger" id="error" role="alert">
                {{message}}
            </div>
            {{/if}}
            {{#if successMessage}}
            <div class="alert alert-success" id="error" role="alert">
                {{successMessage}}
            </div>
            {{/if}}
        </p>
    </div>
</body>


<script>
    // JavaScript code to scroll to the error message
    document.addEventListener('DOMContentLoaded', function () {
        var errorMessage = document.getElementById('error');
        if (errorMessage) {
            errorMessage.scrollIntoView({ behavior: 'smooth', block: 'start' });
        }
    });
</script>

</html>
