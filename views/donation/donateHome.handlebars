<style>
    .container-1 {
        min-width: 450px;
        margin: 0 auto;
        padding: 20px;
        border: 1px solid #ccc;
        border-radius: 10px;
        position: absolute;
        top: 45%;
        left: 50%;

        transform: translate(-50%, -50%);

    }

    .form-group-1 {
        margin-bottom: 20px;
    }
</style>

<form action="/donate" method="POST">
<div class="container-1">
    <h2 class="text-center mb-4">Food Donation Shop</h2>
    <p> Thank you for considering donating to one of our pantries. Below, you can see what items we are currently accepting. This changes from time to time. Please check back often to see what we are accepting!</p>
    <div class="row justify-content-center">
        <select name="foodItems" class="form-control mb-4">
            {{#each foodItems}}
                <option value="{{neededItem}}">{{neededItem}} - Type: {{typeOfItem}}</option>
            {{/each}}
        </select>
        <label for="quantity">Quantity:</label>
        <input type="number" id="quantity" name="quantity" class="form-control mb-2" min="1" max="10">
        <label for="expiry">Expiry Date:</label>
        <input type="date" id="expiry" name="expiry" class="form-control mb-2">
        <button class="btn btn-primary donate-button">Donate</button>
    </div>
</div>
</form>